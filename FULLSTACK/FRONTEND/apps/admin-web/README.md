# Admin Web

Placeholder workspace for the future dedicated admin frontend.

Current admin UI is still served from the shared server-rendered user-web resources and should be extracted here incrementally.
