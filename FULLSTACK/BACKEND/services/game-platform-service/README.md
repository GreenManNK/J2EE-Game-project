# game-platform-service

Primary Spring Boot service that currently hosts gameplay, account, social, admin, and realtime flows.

This is the transitional service extracted into the backend monorepo layout. Future service decomposition should split stable domains out of here instead of adding more root-level modules.
