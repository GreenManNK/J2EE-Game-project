# Infra

Workspace for deployment, environment provisioning, and platform automation artifacts.

Current runtime scripts still live in `FULLSTACK/BACKEND/scripts` because they are tightly coupled to the Spring Boot service lifecycle. Infrastructure assets can be migrated here incrementally without blocking application development.
